# Prevent canvas from falling back to a source build if no pre-built binary is found.
# On Node 20 linux-x64, canvas 2.11.2 has a pre-built binary so this is a safety net only.
canvas_binary_host_mirror=https://github.com/Automattic/node-canvas/releases/download/
